# GitHub Ready

Prepared from Genius Property V21.1 security-hardened build.

- Vite base path is configurable with `VITE_BASE_PATH`.
- GitHub Pages workflow included.
- No local `.env` or secrets included.
- Frontend Firebase configuration is injected through GitHub Actions variables.
- Backend/API secrets must remain on the backend.
