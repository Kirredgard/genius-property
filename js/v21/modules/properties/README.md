# Module Properties V21

Domaine migré : biens immobiliers.

API temporaire :

- `window.GPV21Properties.init()`
- `window.GPV21Properties.save(payload)`
- `window.GPV21Properties.archive(propertyId)`
- `window.GPV21Properties.state()`

Cette façade permet de continuer la migration sans rupture brutale du legacy.
